#include <stdio.h>
int main(void)
{
    int seconds = 0, minutes = 0, hours = 0, days = 0, weeks = 0;
    printf("Please enter a number of seconds:");
    scanf("%d",&seconds);
    printf("%d",seconds);
}

